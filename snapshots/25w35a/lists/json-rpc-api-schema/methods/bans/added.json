{
  "description": "Player was added to ban list",
  "name": "notification:bans/added",
  "params": [
    {
      "name": "player",
      "required": true,
      "schema": {
        "$ref": "#/components/schemas/user_ban"
      }
    }
  ]
}